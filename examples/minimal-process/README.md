# examples/minimal-process

This directory will later contain the first executable DeepPlant semantic model:
a small, real process fragment written as YAML and loadable by the DeepPlant
CLI.

Do **not** invent the YAML schema here. The schema is designed deliberately in
the next task, driven by example fragments such as this one. Until then this
directory stays intentionally empty apart from this note.
